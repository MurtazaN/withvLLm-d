# Secrets — every .env variant is ignored, but the template is committed.
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Build / packaging
build/
dist/

# Frontend build artifacts (regenerated by Dockerfile stage 1; produced
# locally by `cd src/blue_lantern/frontend/styles && npm run build`).
# package-lock.json is intentionally NOT ignored — commit it for
# reproducible image builds.
src/blue_lantern/frontend/static/
src/blue_lantern/frontend/styles/node_modules/

# Benchmark output (regenerated each run)
src/blue_lantern/benchmark/results/*.csv

# Data
data/

# Opencode and graphify
AGENTS.md
graphify-out/

# GCP 
.secrets/